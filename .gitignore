# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# source image drops at repo root (Ideogram/reference photos).
# Real, optimized site assets live under /public and are committed.
/*.png
/*.jpg
/*.jpeg
/*.JPG
/*.webp

# Generation working files (variants live locally; only conformed winners ship)
scripts/out/

# Agent worktrees (full checkouts; eslint ignores them too)
.claude/worktrees/

# Scratch scripts agents and people write in the repo root (_foo.tmp.mjs, ...)
_*.tmp.*
